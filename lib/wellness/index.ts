/**
 * Single namespace for feeding + sleep engines (task 32).
 * Import from `@/lib/wellness` instead of reaching into engines directly in new code.
 */
export * from "@/lib/feeding-engine";
export * from "@/lib/sleep-engine";
