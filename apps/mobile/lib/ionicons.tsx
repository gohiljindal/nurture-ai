/**
 * Single import path for Ionicons. Use a relative path so Metro always resolves
 * the built module even when `@expo/vector-icons` package root fails to resolve.
 */
// eslint-disable-next-line import/no-relative-packages
import Ionicons from "../node_modules/@expo/vector-icons/build/Ionicons";

export { Ionicons };
export default Ionicons;
