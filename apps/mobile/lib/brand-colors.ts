/**
 * Brand color hex values for places Tailwind can't reach (ActivityIndicator,
 * tab bar tint, RefreshControl, vector icons). Keep in sync with `brand` in
 * tailwind.config.js.
 */
export const BRAND_PRIMARY_HEX = "#7c78ee"; // brand-500 (lavender)
export const BRAND_PRIMARY_MUTED_HEX = "#6366f1"; // brand-600
